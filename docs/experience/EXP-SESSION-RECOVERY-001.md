# EXP-SESSION-RECOVERY-001: Session Recovery Draft

## Observation

The previous session could complete work, but a later CLI session still needed a file-based entrypoint to recover what happened.

## Lesson

If the active project does not maintain `STATE.md` plus `docs/handoff/LATEST.md`, the recovery path collapses back into chat memory.

## Promotion Note

This is a project-local operational lesson. It belongs in project docs, not in `Foundation-Core` yet.
