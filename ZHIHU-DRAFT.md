# AI 长任务为什么总是越做越偏？我把需求、知识库、验证绑成了一套工作底座

过去一段时间，我在用 AI 持续推进一些周期很长的项目。最明显的感受不是 AI 不会做，而是项目越做越容易偏：需求文档和代码逐渐脱节，前面试过的东西后面又重复试，换一个 agent 接手之后，很多结论又退回到“重新摸索”。

这类问题，单靠聊天记录解决不了。因为聊天记录会丢、会断、会被新的上下文覆盖；而项目真正需要的，是能被后续 agent 直接读取和复用的结构。

后来我把这些问题拆开，做成了一套更像工程底座的结构：需求点、知识条目、计划、验证、审计都不再分散，而是绑定在一起。这套方法，我把它叫做“夯实地基”。

## 一、长任务最怕的，不是不会做，而是越做越偏

AI 开发里最常见的几个坑，我几乎都踩过：

- 需求文档写了很多，实际代码慢慢跑偏
- 前一轮 agent 试出来的结论，下一轮又忘了
- 试错很多，但没有形成可复用知识
- 验证和回归散在各处，最后很难判断到底改对没有

这些问题在短任务里不明显，一旦项目进入多轮迭代、多语言、多场景、多人协作，就会非常伤效率。

## 二、为什么只靠“记住”不够

我一开始也以为，只要让 AI 多看上下文、多记一点就行了。但实际不是。

原因很简单：

1. 上下文会被截断
2. 任务会跨多天
3. 需求会不断细化
4. 经验会分散在不同对话里

所以我需要的不是“更会聊天的 AI”，而是“能把项目结构保存下来的 AI 工作方式”。

## 三、我做的结构：把需求、知识、验证绑起来

现在这套方法的核心，是把一个需求点和相关知识条目绑定起来。

比如：

- 一个需求点对应一个 `REQ-*`
- 这个需求点会链接到相关 `KB-*`
- 方案放在 `PLAN-*`
- 测试结果放在 `VERIFICATION-*`
- 结论和偏差记录放在 `AUDIT-*` 或 `EXP-*`

这样做的好处是，后来的 agent 不需要从零猜，它可以直接顺着文件关系读出：

- 这个需求要达成什么
- 之前尝试过什么
- 哪些结论已经稳定
- 哪些地方还在试验

## 四、为什么我把它叫“夯实地基”

因为它不是一个单点功能，而是一层底座。

它解决的不是某一个具体需求，而是 AI 项目里更底层的问题：

- 长上下文作业后，目标和进度容易失真
- 需求文档和实际实现会慢慢脱节
- 试错过程不沉淀，就会反复踩坑
- 验证和审计没有统一结构，结果就不稳

我后来发现，只要把知识绑定到需求上，很多原本要反复解释的东西，下一轮 agent 直接就能接上。这比单纯堆提示词有效得多。

## 五、一个真实的变化：粤语歌任务

这个方法最明显的效果，出现在我做粤语歌生成的时候。

最开始的问题很多：

- 粤语会跑成普通话
- 咬字不稳
- 风格标签和实际输出不一致
- 同样的提示词，反复试很多次都不稳定

后来我把有效结论整理成知识条目，再把这些知识条目绑定到需求点上，后面的效果就明显稳定了。不是“AI 突然变聪明”，而是它终于有了可继承的工作底座。

这就是我想强调的区别：

- 没有地基，AI 只能靠碰运气
- 有了地基，AI 才能沿着已知结论继续推进

## 六、这套方法适合什么项目

我认为它最适合这几类场景：

- 长周期 AI 开发项目
- 多轮 agent 协作项目
- 需求变化快、验证频繁的项目
- 需要知识沉淀和回退能力的项目

它不适合拿来替代所有管理方式，也不是万能的。它更像一套“让项目不散架”的结构。

## 七、它不是环境克隆，而是工作方法迁移

这点我也想讲清楚。

它不会复制你的登录态、cookie、token、私有附件，也不会替你保留运行时状态。它复制的是：

- 怎么起手
- 怎么规划
- 怎么验证
- 怎么审计
- 怎么把经验沉淀下来

也就是说，它迁移的是方法论，不是机器状态。

## 八、为什么我愿意把它开源

因为我发现，真正有价值的不是某一轮解决了什么，而是“以后不要再掉进同一个坑”。

如果一套工作方式能让后来的 agent 少走弯路、让项目少回退几天、让经验真正沉淀下来，那它就值得公开。

## 九、如果你也在做长期 AI 项目

你大概率也会遇到这些问题：

- 需求改着改着就和代码不一致了
- 新 agent 接手后，总是要重新摸一遍
- 试错很多，但很少形成可复用结论
- 验证做了，但没有形成系统记录

如果是这样，你可以看看这套“夯实地基”的结构。它不是一个炫技项目，而是一套给 AI 长任务用的工作底座。

GitHub 仓库：`hangshi-diji`

## 十、如果你想先用起来，最短路径是什么

我不建议一上来就把整套结构铺满。最短路径其实很简单：

- 先挑一个真实需求，写成一份 `REQ-*`
- 再补一条对应的 `KB-*`
- 方案放进 `PLAN-*`
- 做完以后补 `VERIFICATION-*`
- 如果过程中有偏差，再补 `AUDIT-*` 或 `EXP-*`

先跑通一条链，再慢慢扩展到整个项目。这样你会很快看见它到底有没有用。

## 十一、它最值得被拿去解决什么

我现在越来越确定，这套东西最值得解决的不是“写文档”本身，而是这些老问题：

- 新人接手要重来
- 老结论散落在聊天记录里
- 试错很多，但复用很少
- 说完成了，却拿不出完整证据链

如果你的项目也常常卡在这些地方，那它就不是一个小技巧，而是一层很实用的底座。

## 十二、结尾

所以我后来不再把它看成一个单独的工具，而是把它当成一套工作习惯：让需求、知识、计划、验证和审计彼此咬合，让项目在多轮协作里不至于散掉。

如果你也在做长周期 AI 项目，或者你已经被“越做越偏”折腾过很多次，也许这套结构会对你有用。

仓库名还是那句：`hangshi-diji`。
