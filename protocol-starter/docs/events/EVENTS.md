# Execution Events

The optional `diji` CLI records machine-observable lifecycle events in
`.diji/events.jsonl`.

The event log is evidence of actions and state observations. It is not a
replacement for requirements, verification, reconciliation, or human review.
