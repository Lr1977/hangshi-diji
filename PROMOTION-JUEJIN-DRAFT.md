# 我把 AI 长任务的需求、知识库和验证流程做成了一套可复用底座

做 AI 项目时，短任务通常不难，真正难的是长任务。

项目一旦跨多天、多轮 agent、多次需求调整，就很容易出现几个问题：需求文档和代码慢慢脱节，前面试过的结论后面又忘了，验证记录散在各处，换一个 agent 接手以后还要重新摸一遍。

我后来把这些问题拆成了一套结构，做成了一个开源项目：Hangshi Diji / 夯实地基。

它不是一个运行环境克隆工具，也不是替代项目管理软件。它解决的是 AI 长任务里的工作结构继承问题。

## 核心问题

AI 长任务常见的失败不是突然失败，而是慢慢跑偏：

- requirement 写了很多，但实现逐渐偏离
- 有价值的经验停留在聊天记录里，没有沉淀成知识库
- 后续 agent 不知道前面试过什么，又重复试错
- verification 和 audit 没有统一位置，完成状态缺乏证据链

这些问题用“让 AI 多记一点”解决不了。上下文会截断，会话会断，任务会跨天，经验会分散。

所以我需要的不是更长的聊天记录，而是一套能被后续 agent 直接读取的项目结构。

## 结构设计

夯实地基把项目工作拆成几类稳定文件：

- `REQ-*`: 需求点
- `KB-*`: 稳定知识条目
- `PLAN-*`: 实施方案
- `VERIFICATION-*`: 验证记录
- `AUDIT-*`: 审计和偏差记录
- `EXP-*`: 新经验和实验结论

核心是 requirement-to-knowledge binding：一个需求点不再孤立存在，而是能链接到相关知识、计划、验证和审计。

后续 agent 接手时，不需要从聊天历史里猜，它可以直接顺着文件关系读出：这个需求要做什么，之前试过什么，哪些结论稳定，哪些还只是实验。

## 四层模型

为了避免文档越写越乱，我把它分成四层：

1. `Foundation-Core`: 稳定规则和长期协议
2. `Foundation-Project`: 当前项目状态、需求、计划、验证目标
3. `Foundation-Learnings`: 实验、证据、待升级经验
4. `Foundation-Deprecated`: 已废弃但保留追溯价值的内容

这套分层有一个重要原则：core 不轻易改，新经验先进 learnings，稳定以后再升级为 KB 或 core 规则。

## 工作流

每次大改都走固定 loop：

`plan -> implement -> verify -> audit -> report`

对应中文就是：方案、实施、验证、审计、归档。

这样做的意义是，每次完成不只留下代码，还留下为什么这么做、怎么验证、有没有偏差、下次怎么接手。

## 一个真实场景

我最早是在一个 Suno 音乐项目里反复遇到问题：粤语生成不稳定，提示词试过很多次，但每轮 agent 接手后都需要重新理解。后来我把有效结论沉淀为知识条目，再绑定到需求点上，后面的推进明显稳定了。

这不是 AI 突然变聪明，而是它终于有了可继承的项目上下文。

## 适合什么项目

这套结构适合：

- 长周期 AI 开发项目
- 多轮 agent 协作项目
- 需求变化快、验证频繁的项目
- 需要知识沉淀和回退能力的项目

不适合：

- 一次性短任务
- 不需要交接的小脚本
- 只想复制登录态、cookie、token、运行环境的场景

它迁移的是工作方法，不是机器状态。

## 最短落地方式

不要一上来铺满全部目录。最短路径是：

1. 选一个真实需求，写成 `REQ-*`
2. 给它绑定一条 `KB-*`
3. 写一份 `PLAN-*`
4. 做完后补 `VERIFICATION-*`
5. 如果有偏差，补 `AUDIT-*` 或 `EXP-*`

先跑通一条链，再扩展到整个项目。

## 开源地址

项目名：Hangshi Diji / 夯实地基

GitHub: https://github.com/Lr1977/hangshi-diji

如果你也在做长周期 AI 项目，欢迎直接拿 starter 结构试一下，也欢迎提 issue 挑问题。
